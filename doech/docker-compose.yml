services:
  clickhouse:
    image: clickhouse/clickhouse-server:latest
    container_name: doech_clickhouse
    ports:
      - "8123:8123"
      - "9000:9000"
    volumes:
      - clickhouse_data:/var/lib/clickhouse
    environment:
      CLICKHOUSE_USER: default
      CLICKHOUSE_PASSWORD: default 
      CLICKHOUSE_DB: default
    
volumes:
  clickhouse_data:
    driver: local